# Bundle assembly report

Generated by `tests/report.py`. Regenerate with `pytest --update-golden`.

`from` names the directory each file came from. Layers are applied in the
order listed, so a later one overwrites a file of the same name from an
earlier one. `mirrored` means the file was already in the bundle from the
copy of the whole framework tree, before any project was placed.

## Reference forms

How each `source:` in the pipeline was written, and what it resolved to.

    local:lookup                              ->  <consumer>/sources/lookup
    local:store                               ->  <consumer>/sources/store
    local:store-tuned                         ->  <consumer>/sources/store-tuned
    local:cache                               ->  <consumer>/sources/cache
    local:service                             ->  <consumer>/sources/service
    local:smoke-test                          ->  <consumer>/sources/smoke-test
    propeller:smoke-test                      ->  <framework>/platform/projects/smoke-test
    propeller://platform/projects/smoke-test  ->  <framework>/platform/projects/smoke-test
    local:adjacent-implicit                   ->  <consumer>/platforms/main/projects/adjacent-implicit
    local:adjacent-named                      ->  <consumer>/platforms/main/projects/adjacent-named

## lookup

Nothing particular. It exists so later steps have outputs to reference by name.

    written  source: local:lookup
    path     platform/projects/lookup
    layers   source(<consumer>/sources/lookup)

    justfile              from source
    project.yaml          from source
    terraform/main.tf     from source
    terraform/outputs.tf  from source

## store-one

Deployed under a different name than its source directory, so it needs a directory of its own rather than sharing one with the other projects built from `store`. It declares two overlays, an org-wide one and its own, which must be applied in the order written so the second wins where both hold the same file.

    written  source: local:store
    path     platform/projects/store-one
    layers   source(<consumer>/sources/store) -> overlay(<consumer>/org-overlays/store-one) -> overlay(<consumer>/overlays/store-one)

    justfile                        from source
    project.yaml                    from source
    terraform/config.auto.tfvars    from overlay 2
    terraform/main.tf               from source
    terraform/outputs.tf            from source
    terraform/override.auto.tfvars  from overlay 2
    terraform/sizes.tf              from source
    terraform/variables.tf          from source

## store-two

A second project from the same source directory, which must not overwrite the first. Written `local:store`, naming the consumer directory outright instead of relying on search order.

    written  source: local:store
    path     platform/projects/store-two
    layers   source(<consumer>/sources/store)

    justfile                      from source
    project.yaml                  from source
    terraform/config.auto.tfvars  from source
    terraform/main.tf             from source
    terraform/outputs.tf          from source
    terraform/sizes.tf            from source
    terraform/variables.tf        from source

## store-one-archive

Its overlay directory name begins with the same characters as `store-one`'s. Overlays are matched by directory name, so each project must receive only its own.

    written  source: local:store
    path     platform/projects/store-one-archive
    layers   source(<consumer>/sources/store) -> overlay(<consumer>/overlays/store-one-archive)

    justfile                        from source
    project.yaml                    from source
    terraform/config.auto.tfvars    from source
    terraform/main.tf               from source
    terraform/outputs.tf            from source
    terraform/override.auto.tfvars  from overlay
    terraform/sizes.tf              from source
    terraform/variables.tf          from source

## store-tuned-one

Its source directory holds two files and declares `base: store`. Every other file must come from `store`, and where both directories hold the same file the one in `store-tuned` must win.

    written  source: local:store-tuned
    path     platform/projects/store-tuned-one
    layers   base(<consumer>/sources/store) -> source(<consumer>/sources/store-tuned)

    justfile                      from base
    project.yaml                  from source
    terraform/config.auto.tfvars  from source
    terraform/main.tf             from base
    terraform/outputs.tf          from base
    terraform/sizes.tf            from base
    terraform/variables.tf        from base

## cache-one

One source directory, no base and no overlay. The simplest placement.

    written  source: local:cache
    path     platform/projects/cache-one
    layers   source(<consumer>/sources/cache)

    justfile                from source
    project.yaml            from source
    terraform/main.tf       from source
    terraform/outputs.tf    from source
    terraform/variables.tf  from source

## service-one

Its source directory has no terraform subdirectory, so nothing may assume one exists. It also holds a `dist/` and a `node_modules/`, which must be left out of the bundle: projects that need them rebuild in CodeBuild, so shipping a local build would risk deploying a stale one.

    written  source: local:service
    path     platform/projects/service-one
    layers   source(<consumer>/sources/service)

    justfile      from source
    project.yaml  from source

## local-smoke

Two directories on disk hold a project named `smoke-test`, one in the consumer and one in the framework. Written without a prefix, the consumer's must be used.

    written  source: local:smoke-test
    path     platform/projects/local-smoke
    layers   source(<consumer>/sources/smoke-test)

    justfile      from source
    project.yaml  from source

## framework-smoke

The same ambiguity, written `propeller:platform/projects/smoke-test` to take the framework's directory instead.

    written  source: propeller:smoke-test
    path     platform/projects/framework-smoke
    layers   source(<framework>/platform/projects/smoke-test)

    justfile                from source
    project.yaml            from source
    terraform/main.tf       from source
    terraform/outputs.tf    from source
    terraform/providers.tf  from source
    terraform/variables.tf  from source
    terraform/versions.tf   from source

## framework-smoke-legacy

The same reference in the older `propeller://` spelling, which must resolve to the same directory.

    written  source: propeller://platform/projects/smoke-test
    path     platform/projects/framework-smoke-legacy
    layers   source(<framework>/platform/projects/smoke-test)

    justfile                from source
    project.yaml            from source
    terraform/main.tf       from source
    terraform/outputs.tf    from source
    terraform/providers.tf  from source
    terraform/variables.tf  from source
    terraform/versions.tf   from source

## smoke-test

Its name matches the framework directory it comes from, so it is already in the bundle from the copy of the whole framework tree and must not be copied again. Only the overlay is added.

    written  source: propeller:smoke-test
    path     platform/projects/smoke-test
    layers   overlay(<consumer>/overlays/smoke-test)

    justfile                        from mirrored
    project.yaml                    from mirrored
    terraform/main.tf               from mirrored
    terraform/outputs.tf            from mirrored
    terraform/override.auto.tfvars  from overlay
    terraform/providers.tf          from mirrored
    terraform/variables.tf          from mirrored
    terraform/versions.tf           from mirrored

## adjacent-implicit

Has no `source:` at all. It is found by matching its name against the projects directory beside the pipeline, which is where a consumer puts a project used by one pipeline only.

    written  source: local:adjacent-implicit
    path     platform/projects/adjacent-implicit
    layers   source(<consumer>/platforms/main/projects/adjacent-implicit)

    justfile      from source
    project.yaml  from source

## adjacent-named

The same directory, reached by writing the name out as `source:`. Resolving by name and resolving implicitly must land in the same place.

    written  source: local:adjacent-named
    path     platform/projects/adjacent-named
    layers   source(<consumer>/platforms/main/projects/adjacent-named)

    justfile      from source
    project.yaml  from source
