propeller:
  version: "PROPELLER_VERSION_PLACEHOLDER"
  repo: "HelixCloud-ch/landing-zone-propeller"

pipeline: {}
  # Remap targets to different accounts
  # targets:
  #   operations-baseline: sandbox

  # Use local project sources instead of the framework's
  # overrides:
  #   - project: operations-baseline
  #     source: "./landing-zone/projects/my-custom-baseline"

  # Add custom projects to existing stages
  # additions:
  #   - stage: baseline
  #     step:
  #       project: my-project
  #       source: "./landing-zone/projects/my-project"
  #       target: operations
  #       outputs:
  #         - name: my-project.result
  #           var: result

  # Remove framework projects
  # removals:
  #   - project: security-hub

  # Reorder stages (must list all)
  # stage_order:
  #   - identity
  #   - baseline
