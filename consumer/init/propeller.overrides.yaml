propeller:
  version: "PROPELLER_VERSION_PLACEHOLDER"
  repo: "HelixCloud-ch/landing-zone-propeller"

pipeline:
  # Override target accounts (remap logical targets to different accounts)
  # targets:
  #   hello-operations: sandbox

  # Override framework projects with local versions
  # overrides:
  #   - project: hello-operations
  #     source: "./projects/my-custom-ops"

  # Add custom projects to existing stages
  # additions:
  #   - stage: operations
  #     step:
  #       project: my-project
  #       source: "./projects/my-project"

  # Remove framework projects you don't need
  # removals:
  #   - project: hello-management

  # Reorder stages (must list all stages)
  # stage_order:
  #   - management
  #   - operations
