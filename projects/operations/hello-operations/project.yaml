name: hello-operations
description: Hello world for the operations account (script-based)
target: operations

deploy:
  type: script

inputs: []

outputs:
  - key: /propeller/hello-operations/message
    ref: message
